name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Project
      uses: actions/checkout@v2
    - name: Lint Project
      run: ./gradlew ktlintCheck
    - name: Build Project
      run: ./gradlew build
